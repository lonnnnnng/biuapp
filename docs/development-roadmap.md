# 开发里程碑

## M1：原生播放骨架

- Compose 主界面、推荐列表占位数据和底部迷你播放栏。
- MediaSessionService、媒体通知、上一首/下一首/暂停恢复。
- Bilibili 请求头隔离和 WBI 签名。
- 播放队列与一次性结束事件测试。

完成标准：Debug APK 可安装；切到后台后继续播放；通知栏可控制。

## M2：真实 Bilibili 播放

状态：核心实现完成（2026-07-29）；登录后的真实账号态等待用户手动验收。

- [x] WebView/Cookie 登录原型。
- [x] `/x/web-interface/view` 获取视频和分 P；单 P 直接播放，多 P 在 M3 通过分 P 选择器构建顺序播放队列。
- [x] WBI nav key 获取与六小时缓存。
- [x] DASH 地址解析、最高可用音质自动降级、失效重试和备用 CDN 切换。
- [x] 首批音乐推荐接口已完成，后续由 M12.1 的综合热门、每周必看和全站排行替换；WBI 视频搜索继续保留。

完成标准：真实账号可以从推荐或搜索点击并播放 Bilibili 音频。

实机证据：

- Redmi Note 8 Pro（`wsvwypiz7xwslvl7`）安装、冷启动和真实接口列表正常。
- 音乐区封面、`Jay Chou` 搜索结果和搜索高亮清洗正常。
- 推荐音频与搜索音频均进入 Media3 `PLAYING`，媒体会话包含真实标题、UP 主和码率。
- 音频默认按无损、杜比、标准 AAC 和兼容流的顺序选择最高可用轨；后台地址失效刷新沿用同一自动降级策略。
- 切换到其他应用后继续播放；媒体通知存在，系统媒体键可暂停并恢复。
- Bilibili 手机号登录页在 WebView 中正常加载；未代用户输入账号凭据。
- 设备原生网络断开，测试使用临时 ADB reverse 代理；测试后系统代理和端口转发均已清理。

## M3：账号音乐库

状态：第一条纵向切片完成（2026-07-29）；真实账号内容等待用户登录后验收。

- [x] 通过 nav 识别登录态和账号 mid。
- [x] “我创建的”“我收藏的”、普通收藏夹、视频合集和在线历史首屏只读浏览。
- [x] 稍后再看保持不提供页面入口；仓库仅保留 API 兼容能力。
- [x] Room 本地播放历史、播放次数、最后进度和重启后续播。
- [x] 多 P 视频分集选择、从任一 P 起播和 Media3 顺序队列。
- [x] 大型分 P 队列渐进解析：选中 P 先播，前后分 P 最多两路后台补齐，新播放取消旧任务。
- [x] 底部播放器在多 P 播放时显示当前 P 序号和名称，切 P 后随 Media3 元数据同步更新。
- [x] 迷你播放器实时进度、缓冲显示和拖动跳转。
- [x] 播放期间每 5 秒保存本地进度，暂停和结束时立即保存。
- [ ] 在线历史分页、搜索、单条删除和清空。
- [ ] 收藏夹新建、重命名、删除及视频加入/移出。
- [ ] Bilibili 播放心跳上报及用户开关。
- [x] Room 播放队列持久化与进程重启恢复。
- [x] 全屏播放页。
- [ ] 歌词搜索、时间轴展示与本地缓存。

第一阶段模拟器证据：

- `Pixel_9`（`emulator-5554`）可见窗口冷启动，真实音乐区推荐加载正常。
- 调整 Web UA 与桌面端一致后，MediaSession 进入 `PLAYING`，DASH 主机请求不再返回 403。
- 暂停后 Room 保存 `lastPositionMs=41562`、`durationMs=178514`、`playCount=1`。
- 强制停止并冷启动后，本地历史仍显示 `0:41 / 2:58`；点击后从约 44.9 秒继续播放并更新为播放 2 次。
- 未登录状态下收藏夹、稍后再看和在线历史显示登录提示，本地历史可独立使用。
- 真实 100 P 视频的选择面板可滚动展示 P 序号、分集标题和时长；真实 2P 视频从 P2 起播时 MediaSession 队列 `size=2`、活动索引为 1。
- P2 可通过上一首切回 P1，P1 自然结束后自动顺序进入 P2；旧 P 的 Room 进度立即保存为 `229760 / 229760 ms`。
- 进度条拖动使 MediaSession position 从约 `59.6s` 跳到 `174.5s`，UI 同步显示 `2:53 / 3:49`；P2 播放期间 Room 周期进度更新为约 `34.9s / 706.4s`。
- 真实 100 P 视频从 P2 点击后约 `0.7 秒` 进入 `PLAYING`，起播时队列约 4 至 5 项，3 秒后增长到 25 至 27 项；切到桌面后仍在后台补齐至 100 项，不再等待全部 DASH 地址解析。
- 100 P 补齐期间切换到单 P 视频后，新队列连续观测均保持 `size=1`，旧任务未向新队列追加媒体项。
- 播放中横竖屏重建 Activity 后 MediaSession 连续保持 `PLAYING`、队列始终为 1 项，进度继续推进；控制器重连没有丢失或重复媒体项。
- 用户指定的“音频”收藏夹第一条确认是 200P：从 P2 点击后 `916 ms` 进入 `PLAYING`，队列从 6 项增长到重连后的 25 项，并在桌面后台完整补齐至 200 项；面板末项直接核验为 P200。
- 同目录第二条单 P 在从 P192 的 14 项渐进队列切入后立即稳定为 1 项，5 秒后仍为 1；进度轨道点击跳到 `3:39:38`，继续拖动后同步到 `5:00:21 / 7:19:45`。
- 多 P 从 P2 切到 P3 时，进度条上方依次显示 `P2 · 002. 海阔天空-Beyond`、`P3 · 003. 郑中基-无赖`；切换到单 P 后恢复显示作者与音质。
- 本轮验收结束后 MediaSession 已暂停，`logcat -b crash` 中 `FATAL EXCEPTION=0`。
- Room 从数据库 v4 原地迁移到 v5，新增队列状态和媒体项表；已选 UP、3 个已完成音频、33,879,772 字节暂停断点及既有视频任务均保留。
- 本地音乐 8 项队列在索引 6、`15,182 ms` 暂停后强制停止，冷启动恢复为同一曲目、同一进度和 8 项队列；恢复后切到下一首仍保持暂停且队列完整。
- 在线队列保存 `BV1heTw6VEJZ:39538855427`、`bvid/cid` 与短期播放 URI，在 `5,058 ms` 暂停后强制停止，冷启动原位恢复且未自动播放。
- 队列时间线变化按 1 秒防抖写入，播放中每 5 秒、暂停、seek 和切歌时立即保存；本轮只操作 `emulator-5554`，`logcat -b crash` 为空。

## M4：本地与下载

状态：第三条纵向切片完成（2026-07-30）。

- [x] MediaStore 本地音乐扫描、运行时音频权限和紧凑列表。
- [x] `content://` 本地音频接入 Media3 队列、后台播放、通知栏和系统媒体切歌。
- [x] SAF 目录授权与本地音乐筛选（Android 10 及以上）。
- [x] 标准 AAC 音频下载、暂停恢复、通知进度和取消清理。
- [x] 下载文件通过 MediaStore 发布到用户可见的 `Music/Biu/`。

第一阶段模拟器证据：

- `Pixel_9`（`emulator-5554`）在 Android 16 权限页显示“music and audio”授权请求。
- MediaStore 扫描到两条测试音频，列表正确显示标题、作者、专辑和时长。
- 点击本地音频后 MediaSession 进入 `PLAYING`，媒体通知显示本地标题；后台通过系统媒体命令成功切换到队列下一首。

第二阶段模拟器证据：

- `Pixel_9` 全库准备 5 条 MediaStore 音频；选择 `Music/BiuFilter` 后只显示 2 条，其中一条位于 `BiuFilter/Child`，`Music/Outside` 对照音频未进入结果。
- 系统 SAF 确认对话授予目录读权限，Preferences DataStore 保存 `external_primary` 与 `Music/BiuFilter/`；强制停止并冷启动后仍恢复目录名称和 2 条筛选结果。
- 点击筛选结果后 MediaSession 进入 `PLAYING`，队列 `size=2`，证明播放队列使用当前筛选结果而不是完整 5 条音乐库。
- 点击“显示全部”后列表恢复 5 条，目录配置清空；最终验收版本重新选择目录并在暂停状态结束，`logcat -b crash` 中没有 Biu 崩溃。

第三阶段模拟器证据：

- Room 从数据库 v2 原地迁移到 v3，新增 17 字段的 `audio_download_tasks`；已有账号配置、本地历史和播放进度保留。
- 下载仅选择最高码率标准 AAC 并保存为 `.m4a`；真实任务完成 204 kbps / 4.6 MB、219 kbps / 76.1 MB 和 129 kbps / 40.3 MB 三组发布。
- 最终文件均通过 MediaStore 发布到 `Music/Biu/`，`mime_type=audio/mp4`、`is_music=1`、`is_pending=0`，可从本地音乐页进入 MediaSession 播放。
- 495.9 MB 长音频在限速下从约 4.0 MB 暂停，继续后增长到 6.5 MB；进程终止前 Room 为 `DOWNLOADING` 且保存 7,084,630 字节，冷启动后自动降级为 `PAUSED`，账号页可继续恢复。
- Range 续传覆盖 `206` 追加、CDN 忽略 Range 返回 `200` 时覆盖重下、`416` 时单次清空断点；任务进度按 1 秒或 512 KB 节流写入 Room 和通知。
- 前台下载服务提供暂停、继续、取消和重试；暂停态与下载态取消都进入 `CANCELLED`，`.part` 临时文件被删除且 Room 进度归零。
- 修复取消命令与 `CoroutineScope.cancel` 同名导致命令协程被误取消的问题；编译字节码与真实按钮均确认调用下载任务取消逻辑。
- MediaProvider 扫描无内嵌标签的 DASH 音频后会回退到文件名和未知艺术家；本地音乐页现在按 `publishedUri` 的 MediaStore ID 用 Room 元数据恢复标题、作者和封面，MediaSession 同步显示正确信息。
- `AudioDownloadService` 为 `exported=false`，Android 15+ 实现 `dataSync` 超时降级；最终网络恢复 `full`，`logcat -b crash` 中没有 Biu 崩溃。

## M5：视频与批量任务

状态：第二条纵向切片完成（2026-07-30）。

- [x] 当前单 P/当前分 P 的视频、标准 AAC 音频双轨下载。
- [x] 同一次 DASH 请求解析两条轨；优先最高可用 AVC，缺失时依次回退 HEVC、AV1。
- [x] 音频和视频共用 Range 断点传输器，覆盖 `206` 追加、忽略 Range 的 `200` 覆盖和 `416` 单次清空重试。
- [x] `MediaExtractor + Media3 Mp4Muxer` 按时间戳交错写入，无转码合并 MP4。
- [x] Room v4 独立视频任务、前台服务、暂停/继续/取消/重试和冷启动恢复。
- [x] 全屏播放器“下载当前视频”入口，以及音频/视频下载任务切换面板。
- [x] 通过 MediaStore 发布到用户可见的 `Movies/Biu/`。
- [x] 收藏夹批量下载、并发限制、失败任务批量重试和网络约束。

第一阶段模拟器证据：

- `Pixel_9`（`emulator-5554`）从数据库 v3 原地迁移到 v4，新增独立 `video_download_tasks`；既有音频任务、本地历史和账号配置保留。
- 真实任务 `BV1PK4y1b7dt:201056987` 解析为 `1080p · AVC`，最终文件发布到 `Movies/Biu/`，大小 `66,769,298` 字节、时长 `188,352 ms`。
- 成品经 `ffprobe` 确认为 H.264 High 1920×1080、AAC-LC 48 kHz 双声道；`ffmpeg` 解码前 5 秒无错误。
- 第二条真实任务在音频轨阶段暂停并保留断点；强制停止应用、冷启动后自动降级为 `PAUSED`，随后取消成功，视频轨、音频轨和合并临时文件均被删除，UI 不再显示旧字节。
- 共享断点传输器回归音频下载：从 `112.4 MB / 495.9 MB` 暂停，继续后增长到 `116.2 MB`，取消后进入 `CANCELLED` 且私有音频临时目录为空。
- 本轮只向 `emulator-5554` 定向安装；视频/音频下载服务验收后均停止，模拟器网络保持 `VALIDATED`，`logcat -b crash` 为空。

第二阶段模拟器证据：

- “音频”收藏夹全量读取 34 项接口数据，过滤失效稿件和非普通视频后得到 29 个可下载资源；弹层默认全选，支持音频/视频、全选和逐项选择，创建按钮固定在底部。
- 选择用户指定的第二条单 P 资源后真实创建音频任务 `BV1fYgYzNEYj:31173904742`，总大小为 `519,967,934` 字节；暂停后 Room 记录 `33,879,772` 字节，私有断点文件实际为 `33,920,678` 字节。进度按时间或字节节流写库，因此 UI 允许短暂落后，恢复下载以文件实际长度继续。
- Pixel_9 默认网络为已验证的计费蜂窝网络。任务下载到约 28.1 MB 时开启“仅 Wi-Fi 下载”，任务保持 `QUEUED` 且连续观察字节不再增长，前台通知显示“等待 Wi-Fi 或其他非计费网络”。
- 关闭网络约束后任务自动继续，最终在约 32.3 MB 手动暂停；强制停止并冷启动后仍为 `PAUSED`，断点文件保留，网络偏好恢复为测试前的“任意已验证网络”。
- JVM 测试覆盖收藏夹 `has_more` 分页、单 P/多 P 任务展开、音频与视频批量入队、已完成/运行任务跳过、失败任务断点保留及一键重试；详情解析并发上限为 3，同类型服务保持单任务 FIFO。
- 本轮只操作 `emulator-5554`，Redmi `wsvwypiz7xwslvl7` 未安装、未启动、未修改；最终 `logcat -b crash` 中没有 Biu 崩溃。

## M6：发布质量

状态：进行中；应用内自动更新已完成首版，`0.1.6` 音频下载通知补丁已通过模拟器验收并发布。

- [x] GitHub Release 检查、下载和系统安装器链路。
- [x] 修复音频下载完成后前台进度通知残留，并发布 `0.1.6` 补丁版。
- [ ] 模拟器和真实设备回归。
- [ ] 崩溃、网络、耗电和后台限制专项测试。
- [ ] 签名、版本、隐私说明、许可证清单和发布流水线固化。

`0.1.6` 模拟器证据：

- `Pixel_9`（`emulator-5554`）从全屏播放器真实下载 44 秒分 P 音频“高级爱情故事”，Room 任务 `BV1dqd7BjEev:37593417601` 完成为 `545,773 / 545,773` 字节并发布到 MediaStore。
- 完成通知显示“下载完成 · 97 kbps”，flags 为 `ONLY_ALERT_ONCE|AUTO_CANCEL`，不再包含 `ONGOING_EVENT` 或 `FOREGROUND_SERVICE`。
- `AudioDownloadService` 完成后退出，通知可以横向清除；正常的 `PlaybackService` 保持运行，`logcat -b crash` 为空。

## 后续纵向推进计划

计划确认日期：2026-07-31。每个阶段必须形成可安装、可操作、可独立验收的用户链路，不按 UI、网络和数据库横向拆开交付。

### M7：播放器核心操作

状态：已完成，目标版本 `0.1.7`。

- [x] 顺序播放、列表循环、随机播放、单曲循环以及模式持久化。
- [x] `0.5x` 至 `2.0x` 七档播放倍速及用户选择持久化。
- [x] 队列删除单项、清空、设为下一首；多 P 队列遵守同一套操作规则。
- [x] 全屏播放器、MediaSession、通知栏和锁屏共享同一播放状态。

完成标准：重启后播放模式和倍速保持；队列编辑、多 P 切换和后台媒体控制均通过模拟器验收。

`0.1.7` 模拟器证据：

- `Pixel_9`（`emulator-5554`）依次切换随机播放、单曲循环、顺序播放和列表循环，MediaSession 状态与全屏播放器一致。
- 倍速切换到 `1.5x` 后 MediaSession 实际报告 `speed=1.5`；强制停止并冷启动后，列表循环和 `1.5x` 均恢复。
- 4 项多 P 队列将 P1 设为下一首后变为 `P2 -> P3 -> P4 -> P1`；移除 P2 后保持 `P3 -> P4 -> P1`，当前 P4 未被错误切换。
- 强制停止并冷启动后，编辑后的 3 项队列、当前媒体项、播放模式和倍速全部恢复。
- 清空队列的确认弹窗正常；确认后 Room 中 `playback_queue_state` 与 `playback_queue_items` 均为 0。
- 应用退到后台并熄屏后系统为 `Asleep`，MediaSession 仍保持 `PLAYING` 和 `speed=1.5`，`PlaybackService` 继续作为前台服务运行。
- `:app:testDebugUnitTest`、`:app:lintDebug` 和 `:app:assembleDebug` 均通过，`logcat -b crash` 为空。
- 正式签名的 `BiuApp-v0.1.7.apk` 安装到隔离的 `Pixel_9`（`emulator-5556`）后，真实推荐播放进入 `PLAYING`；列表循环和 `1.5x` 可操作，强制停止并冷启动后队列、进度、模式和倍速恢复，crash buffer 为空。

### M8：在线历史闭环

状态：已完成，原目标版本 `0.1.8`，合并随 `0.1.10` 发布。

- [x] 游标分页、标题/UP 主搜索、单条删除和清空。
- [x] 在线历史与 Room 本地历史保持独立分区和状态。
- [x] 增加 Bilibili 播放心跳上报及用户开关。

完成标准：真实账号完成分页、搜索和历史进度同步；删除与清空通过真实确认流程及 MockWebServer 请求契约验收，避免破坏账号数据；登录失效、空数据、网络及 API 错误均有明确处理路径。

`0.1.8` 模拟器证据：

- `Pixel_9`（`emulator-5554`）保留真实登录账号后加载在线历史，连续滚动越过首批 20 条仍可读取后续内容；搜索 `IM` 只返回标题或 UP 主匹配项。
- 在线历史顶部固定显示搜索、清空和“记录播放历史”开关；单条删除与清空均先显示确认弹窗，且文案明确不会影响 Room 本地历史。为避免破坏真实账号数据，模拟器验收只检查确认流程，最终 POST 由 MockWebServer 覆盖 `kid/csrf`、表单编码及接口错误。
- MockWebServer 同时覆盖空游标页、`-101` 登录失效、HTTP `503` 网络错误和 `-111` API 错误；登录失效处理只清理账号在线区，Room 本地历史保持独立可用。
- 上报开关关闭和重新开启后分别经过强制停止、冷启动，状态均由 DataStore 正确恢复；最终保持开启。
- 真实视频从 `PLAYING` 到 `PAUSED` 后 Bilibili 在线历史进度更新为 `0:05`，与 MediaSession 的 `5080ms` 一致；修复多余开始观看接口导致的 `START -400` 后，上报警告为空。
- `PlaybackService` 验收时保持前台服务，MediaSession 标题、作者、进度和暂停态一致，`logcat -b crash` 为空。
- `:app:testDebugUnitTest`、`:app:lintDebug` 和 `:app:assembleDebug` 均通过。

### M9：收藏夹管理闭环

状态：已完成（2026-07-31），原目标版本 `0.1.9`，合并随 `0.1.10` 发布。

- [x] 新建、重命名和删除收藏夹。
- [x] 将视频加入指定收藏夹或从收藏夹移出。
- [x] 保持“我创建的”“我收藏的”、普通收藏夹和视频合集边界清晰。
- [x] 收藏夹详情按页续载，失效资源整页过滤后继续读取，快速切换时忽略迟到响应。

完成标准：真实账号完成收藏夹 CRUD 和视频加入/移出；分页、失效资源、单 P 和多 P 播放链路不回退。

当前验证证据：

- Smart Search 与桌面端只读实现共同核验 `/x/v3/fav/folder/add|edit|del`、`/x/v3/fav/resource/deal` 的 Cookie、CSRF 和表单参数契约。
- MockWebServer 覆盖收藏夹 CRUD、视频加入/移出、普通收藏夹分页、失效资源过滤，以及普通收藏夹与视频合集的接口边界。
- `Pixel_9`（`emulator-5554`）保留真实账号态安装最新 Debug APK，完成临时收藏夹新建 `Biu-M9-QA-0731`、重命名、视频加入、重复加入幂等、从详情移出和删除；删除后账号页不再显示临时收藏夹。
- 重复加入通过 `/x/v3/fav/folder/created/list-all?rid=...` 的 `fav_state` 识别为已存在，提示“已在…中”且数量保持 `1`；移出后详情显示 `0 项内容` 和“暂无内容”。
- 收藏夹详情按服务端 `has_more` 续载，失效资源过滤后继续翻页；普通收藏夹和视频合集分别使用 `/x/v3/fav/resource/list` 与 `/x/space/fav/season/list`，详情 `media_count` 从 `data.info` 同步。
- 本轮模拟器 UI 树确认收藏管理入口保持 `48dp` 触控区域；安装、冷启动及 CRUD 回归后 `adb -s emulator-5554 logcat -b crash` 无崩溃记录。

### M10：歌词

状态：已完成（2026-07-31），目标版本 `0.1.10`。

- [x] 点击歌词按钮后手动确认或编辑歌曲名、歌手，再发起搜索。
- [x] 支持 LRCLIB 多结果选择、时间轴解析、自动滚动和当前行高亮。
- [x] Room 缓存歌词和用户选择，支持重新搜索。
- [x] 单 P 使用资源标题，多 P 使用当前分 P 名称，并彻底移除 Bilibili 字幕接口。

完成标准：有歌词、无歌词、缓存命中和重新选择均可独立验收，歌词失败不影响播放。

当前验证证据：

- Redmi Note 8 Pro（`wsvwypiz7xwslvl7`）验证多 P 默认搜索词、LRCLIB 候选选择、歌词显示和滚动，切换分 P 后按当前 P 名称重新搜索。
- Room 已保存用户选择的 LRCLIB 歌词；冷启动后可按 `bvid:cid` 独立命中，不会混用主标题或其他分 P。
- Android 目录静态扫描无 Bilibili 字幕接口、`subtitle_url` 或 `/x/player/wbi/v2` 歌词读取残留。
- `:app:testDebugUnitTest`、`:app:lintDebug` 和 `:app:assembleDebug` 均通过，真机崩溃日志为空。

### M10.1：在线视频播放

状态：实现完成（2026-07-31），目标版本 `0.1.11`；Redmi 真机已完成本轮竖屏视频和账号/列表调整验收。

- [x] 在线内容默认播放音频，可从正在播放页手动切换当前分 P 的视频。
- [x] Media3 合并独立 DASH 视频轨与标准 AAC 音频轨，切换时保留队列、进度、倍速、循环模式和播放/暂停意图。
- [x] 视频模式在竖屏使用原封面区域的 `16:9` 小窗，横屏随系统方向进入沉浸全屏；共用控制层支持播放/暂停、上一 P/下一 P、进度拖动、倍速、播放列表、画质、方向切换和切回音频。
- [x] 画质菜单来自当前账号实际可用的视频轨；同一清晰度优先 AVC，缺失时回退 HEVC、AV1，切换画质不重置进度。
- [x] 视频只作用于当前分 P；切到其他 P 和冷启动恢复队列时仍以音频为默认模式。
- [x] URL 失效刷新按当前音频/视频模式重建媒体源，并在异步结果返回前二次校验媒体项身份。

完成标准：Redmi 真机完成竖屏小窗、自动旋转横屏沉浸全屏、返回竖屏、画面非空、播放暂停、拖动、倍速、前后 P、至少两档画质、切回音频、后台媒体会话和 crash log 验收。

当前代码级证据：

- JVM 测试覆盖画质去重、指定清晰度选流、AVC/HEVC/AV1 回退、Repository 实际候选列表及音频持久化边界。
- `:app:compileDebugKotlin`、定向 JVM 测试、`:app:lintDebug` 和 `:app:assembleDebug` 已通过。
- Redmi `wsvwypiz7xwslvl7` 已安装最新 Debug APK；竖屏视频区域实测为 `[0,287][1080,895]`，底部工具栏为 `[0,752][1080,895]`，左右无白边和圆角。MTK 真机默认轨已调整为 `720p · AVC`，避开已确认会在正常播放和资源 flush 中原生崩溃的 HEVC 路径。
- 同一真机已确认 MTK AVC 厂商解码器也可能在 `FillThisBuffer`、颜色转换和 `ComponentDeInit` 中崩溃；播放服务现按 30 秒窗口最多两次重建 Player，并使用 `MediaSession.setPlayer` 保持系统控制连接。安装包含未分类错误恢复的 Debug APK 后，视频从 `5:16` 连续穿过复现点播放至至少 `5:57`，MediaSession 始终为 `PLAYING(3), error=null`；视频播放中切换资源后也未停留在 `ERROR(7)`。
- 同一真机已核验账号页五个单行 Tab、收藏夹分组默认收起、下载任务迁入账号页、推荐/历史紧凑搜索和统一列表密度；播放页已移除下载列表入口，AndroidRuntime、MediaCodec 和 Media3 错误筛查均为空。

### M11：用户与关注

- 状态：实现完成（2026-08-01），Redmi `wsvwypiz7xwslvl7` 已完成真实账号验收。
- [x] 用户名称搜索、UP 主空间、按发布时间倒排的投稿列表及分页。
- [x] 独立关注列表、分页、本地筛选、关注和取消关注。
- [x] 首页内容范围复用统一 UP 主数据并持久化 UID；取关成功后同步删除对应首页范围 UID。

完成标准：真实账号可从搜索进入空间、切换关注状态，并与首页已选 UP 配置保持一致。

当前代码级证据：

- Repository 已覆盖用户搜索、空间资料、投稿分页、关系读取和关系修改接口，MockWebServer 合约测试验证 WBI 查询及关注表单。
- 推荐页新增 UP 主入口；UP 主中心提供“用户搜索 / 我的关注”，空间页复用紧凑投稿列表并支持播放和收藏。
- `:app:testDebugUnitTest`、`:app:lintDebug` 和 `:app:assembleDebug` 均通过。
- Redmi 真机已验证 `STN` 用户搜索、空间资料与投稿、已关注状态读取；测试账号 `ZX-STN` 完成“关注 -> 设为首页范围 -> 取关”，取关后按钮恢复“关注”，首页自动恢复默认热门来源。
- 验收结束后 `ZX-STN` 保持未关注、首页 UP 范围为空，AndroidRuntime 崩溃日志为空。

### M12：动态与互动

- 关注 UP 动态流。
- 点赞、取消点赞和带确认的一键三连。
- 操作后状态同步，并处理限流、重复请求和账号风控提示。

完成标准：动态分页和互动操作均使用真实账号验证；重复操作、失败重试和状态回滚行为明确。

当前状态：功能实现完成，真实三连扣币待单独授权验收。

当前代码级证据：

- 底部新增“动态”主页面，读取 `/x/polymer/web-dynamic/v1/feed/all` 的视频动态并使用服务端 `offset/has_more` 连续分页；卡片支持直接进入现有单 P/多 P 播放链路。
- 动态点赞使用 `/x/dynamic/feed/dyn/thumb` 的 JSON 正文与 CSRF 查询参数；界面先乐观更新，失败时仅回滚仍属于本次请求的快照，同一动态写入期间禁止重复提交。
- 一键三连使用 `/x/web-interface/archive/like/triple`，发送前显示视频名称、投币和默认收藏夹影响，只有显式确认后才提交；成功响应同步点赞状态和计数。
- `-101/-111/-352/-403/-412/-799` 分别映射为登录失效、凭据校验、账号风控和请求频繁提示；Repository 合约测试及纯状态策略测试覆盖分页、请求格式、乐观更新、回滚和错误提示。
- Redmi `wsvwypiz7xwslvl7` 真实账号已验证动态首屏、连续下滑分页、动态音频播放，以及同一条动态“点赞 10 -> 11 -> 取消 11 -> 10”的可恢复往返；三连确认弹窗已验证并取消，未擅自执行不可逆的真实扣币和收藏。
- `:app:testDebugUnitTest`、`:app:lintDebug` 和 `:app:assembleDebug` 均通过，真机 AndroidRuntime 崩溃日志为空。

### M12.1：热门推荐分页与动态密度

当前状态：功能实现完成，Redmi 真机验收通过（2026-08-01）。

- [x] 默认首页改为“综合热门 / 每周必看 / 全站排行”三个热门页来源。
- [x] 综合热门读取 `pn/ps/no_more`；每周必看按更早期数续载；全站排行固定 Top 100 并在客户端按 20 条分批展示。
- [x] 默认热门列表接近末尾自动加载更多，续页失败时保留已加载内容。
- [x] 配置首页 UP 范围后，为每位 UP 独立维护下一页并限制最多 3 路并发，合并后按发布时间倒排、按 BV 号去重。
- [x] 动态卡片压缩为 `28dp` 圆形头像、同一行作者/时间、`96dp x 54dp` 封面和单行说明，同时保留互动操作触控区域。

完成标准：Redmi 真机可切换三个热门来源并连续上滑；配置 UP 范围后可加载第二批投稿；动态页亮暗主题下无文字重叠，播放与互动入口正常，crash log 为空。

当前验证证据：

- Smart Search 核验综合热门 `/x/web-interface/popular` 的 `pn/ps/no_more`、每周必看 `series/list + series/one` 及全站排行 `/x/web-interface/ranking/v2` 固定 Top 100 语义；未向排行发送无文档依据的 `pn/rid`。
- MockWebServer 测试覆盖三个接口的路径、参数、视频映射、服务端 `hasMore` 和排行客户端分批边界；双轴审查发现并修复续页全重复时可能连续触发请求的问题。
- Redmi `wsvwypiz7xwslvl7` 真实接口验证三个热门标签均可加载；综合热门连续滑过首批 20 条后继续出现下一批内容，全站排行跨过首批 20 条后继续展示后续排名。
- 临时选择 `STN工作室` 后首页切换为“我的关注 / 已选 1 位 UP”，连续滑动跨过首批 30 条并进入第二页的 2025 年投稿；验收后已清空测试范围，恢复默认热门。
- 动态页亮色和暗色均一屏显示约 4 条紧凑卡片，作者/发布时间、封面、标题、单行说明、播放、点赞和三连无重叠；主题最终恢复“跟随系统”。
- `:app:testDebugUnitTest`、`:app:lintDebug` 和 `:app:assembleDebug` 均通过；真机 AndroidRuntime 与 crash buffer 无应用崩溃。

### M12.2：推荐 Tab、手势刷新与动态紧凑布局

当前状态：实现完成（2026-08-01），Redmi 真机验收待本轮构建完成后执行。

- [x] 推荐页标题栏移除主题、音质和账号入口，只保留“首页内容范围”和“UP 主搜索”两个 `48dp` 图标入口；动态页标题栏只保留刷新按钮并展示加载态。
- [x] 首页内容范围与 UP 主中心统一为无拖拽把手的全高底部弹层，使用同一套紧凑标题栏、搜索框、列表分隔线和语义色；首页范围的保存操作固定在弹层底部。
- [x] UP 主中心将搜索栏放在“用户搜索 / 我的关注”Tab 上方，Tab 复用推荐与账号页的 `48dp` 下划线选中样式。
- [x] 未配置首页范围时使用账号页同款 Tab 展示“综合热门 / 每周必看 / 全站排行 / 入站必刷”；入站必刷固定列表仅在客户端按 20 条分批展示。
- [x] 配置范围后，每位 UP 独立成为可横向滚动的 Tab，并分别保存投稿、下一页、首屏和续页加载状态；配置增删保留未变化 UP 的缓存。
- [x] 推荐列表和搜索结果支持下拉刷新，默认热门和当前 UP Tab 继续支持接近末尾自动加载更多。
- [x] 动态页使用标题栏刷新、下拉刷新和服务端游标续载；单项使用 `112dp` 宽且与右侧四行实际行高等高的封面、纯日期时间、最多两行标题，以及紧凑互动行。

完成标准：四个默认 Tab 与多个 UP Tab 可切换；下拉刷新可更新当前内容；连续上滑不会串用其他 Tab 游标；动态页一屏密度提升且播放、点赞、三连入口无重叠。

当前代码级证据：

- MockWebServer 覆盖 `/x/web-interface/popular/precious` 路径、无伪分页参数和客户端第二批切片。
- 纯状态测试覆盖配置增删时保留未变化 UP 缓存，以及续页仅推进当前 UP 游标并按 BV 号去重。
- `PullToRefreshBox` 只包裹推荐和动态滚动内容；初始加载、续页加载和下拉刷新保持独立状态。
- 动态标题栏刷新与下拉刷新共用 `loadDynamicFeed(reset = true)`；请求期间禁用刷新按钮并显示 `20dp` 进度指示，避免重复提交。
- UP 主中心从独立全屏页调整为覆盖推荐页的全高弹层，关闭后保留推荐 Tab、列表滚动位置与搜索上下文。

### M13：设置与个性化

状态：第二条纵向切片完成（2026-08-01），目标版本 `0.1.14`。

- [x] 账号菜单新增“界面显示”面板，提供小号、标准、大号三档应用字号和标准、紧凑两档媒体列表密度。
- [x] 字号通过根 `MaterialTheme` 统一派生，推荐、动态、收藏夹、在线/本地历史和本地音乐通过同一密度令牌调整封面、间距和分隔线。
- [x] 两项设置使用独立 Preferences DataStore 保存；缺失或未知值回退标准，冷启动后恢复。
- [x] 推荐与搜索结果支持列表/两列网格切换；网格保留播放、收藏、解析状态和触底续载，其他媒体库保持信息密度更高的列表。
- [ ] 自定义主题色、背景色、圆角和字体家族。
- 不做：设置导入/导出。
- [ ] Bilibili 请求代理、自定义下载目录和封面下载。
- [ ] 菜单显示/隐藏配置。

完成标准：设置在重启后保持；列表/网格切换不丢失播放、收藏或分页行为；代理和下载目录失败不会破坏现有配置。

当前验证证据：

- `DisplayPreferencesPolicyTest` 覆盖字号和密度的默认值、全部已存值及未知值回退。
- Redmi `wsvwypiz7xwslvl7` 当前运行 `0.1.14` Debug APK，账号、收藏夹与本地播放数据保留；“界面显示”面板可选择字号、列表密度和推荐视频布局。
- 强制停止、冷启动后两项选择仍显示为已选；推荐列表在“大号 + 紧凑”组合下保持标题、发布时间、作者、收藏和播放入口无重叠。验收后恢复“标准字号 + 标准列表”。
- 同一 Redmi 已验收推荐网格的双列卡片、收藏与播放入口、多 P 选择、持续滚动，以及强制停止后的网格布局恢复；验收后已恢复列表布局。
- `:app:testDebugUnitTest`、`:app:lintDebug`、`:app:assembleDebug`、`:app:assembleRelease` 均通过，Redmi crash buffer 为空。
- `0.1.14` Release APK 已通过 zipalign、APK Signature Scheme v2/v3、版本和 SHA-256 校验，并继续使用历史正式证书；当前 Redmi 安装的是 Android Debug 证书版本，因此系统禁止在不清数据的前提下用正式证书包覆盖，真机功能验收使用同版本同源码 Debug APK 完成。

### M13.1：手机端 UI/UX 打磨

状态：第一条纵向切片完成（2026-08-06），目标版本 `0.1.16`。

- [x] 推荐与搜索网格按可用宽度自动使用两列、三列或四列。
- [x] 网格播放与收藏移入封面覆盖层，移除底部冗余动作行。
- [x] 动态标题提升为正文层级，点赞与三连扩大触控区域并强化选中态。
- [x] 搜索无结果提供“返回推荐”恢复动作。

验证证据：

- `VideoGridLayoutPolicyTest` 覆盖手机、中等宽度和横屏主内容断点。
- Redmi 真机已验证竖屏两列、横屏三列，以及明暗主题下覆盖按钮的对比度。
- `:app:testDebugUnitTest`、`:app:assembleDebug` 已通过。

### M13.2：列表反馈与触控层级

状态：第一条纵向切片完成并随 `0.1.18` 发布（2026-08-07）。

- [x] 动态卡片将播放区域与点赞、三连操作拆成独立触控和无障碍节点，减少误触。
- [x] 推荐、动态、收藏夹和在线历史的续页统一显示进度指示与“正在加载更多”状态。
- [x] 空状态恢复动作改用低强调 Tonal 按钮，保持内容优先的视觉层级。
- [x] 列表播放入口保持 `48dp` 触控区，视觉按钮统一收敛为 `32dp`。
- [x] 账号页移除音质切换按钮，播放、历史恢复、队列恢复和下载任务统一使用最高可用音质自动降级。

验证证据：

- `:app:testDebugUnitTest`、`:app:assembleDebug` 已通过。
- 静态检查确认动态父级不再包裹互动按钮，续页 footer 统一复用 `ListLoadingFooter`。
- `0.1.17` 正式 APK 使用历史 BiuApp 证书签名，`versionCode=18`、`versionName=0.1.17`，SHA-256 为 `6bb9cd66abf394ed6404f29011e493a11099b42dd230bb7d9f0ea54665d8d14b`。
- `0.1.18` 正式 APK 继续使用同一历史证书签名，`versionCode=19`、`versionName=0.1.18`，SHA-256 为 `c81b55d11be8fad359daf919d6542d75e9a6e0c07721d169ba698fe9ccb9dcb9`。

### M14：正式版质量

- Room 迁移、登录 Cookie、日志和导出文件隐私检查。
- 后台播放、断网恢复、进程重建、大收藏夹、长队列和连续下载压力测试。
- 冷启动、滚动、图片缓存和内存优化；完善发布失败和回滚流程。

完成标准：完整回归、隐私和发布检查清单通过，正式 APK 可验证签名、版本、哈希及在线更新。

## 后续产品路线图

规划日期：2026-08-07。

产品定位：BiuApp 是一款以 Bilibili UP 主歌曲投稿和视频合集为主要内容来源的便携听歌软件。默认使用音频模式，视频播放是当前歌曲的补充能力。后续开发优先完善“管理音乐来源 -> 发现歌曲 -> 整理音乐库 -> 连续收听 -> 离线收听”链路，不扩张为完整 Bilibili 社区客户端。

### M15：音乐库信息架构与 UP 主来源管理

优先级：P0。状态：进行中，统一入口与本地分组纵向切片已完成（2026-08-07）。

- [x] 底部“账号”调整为“音乐库”；登录用户、刷新、退出、下载和更新等账号操作继续集中在右上角用户菜单。
- [x] 将“首页内容范围”和“UP 主搜索和关注”合并为统一的“管理音乐来源”页面。
- [x] 统一展示“用户搜索 / 我的关注 / 首页已选”，搜索、关注、取关和首页来源选择在同一页面完成。
- [ ] 支持为 UP 主添加本地分组或标签，例如翻唱、现场、纯音乐和合集；首页可按分组切换或聚合。
- [x] 音乐库一级结构收敛为“在线 / 本地 / 下载”，收藏夹、历史、本地音乐等作为二级内容切换，避免五个等宽 Tab 在窄屏被压缩。
- [ ] 页面关闭或切换后保留搜索词、选中分组、当前 UP、列表位置和未保存选择。

当前证据：Room 已升级至 v7，新增独立 `creator_groups` / `creator_group_members`，首页范围全量保存不会删除分组；管理页支持创建、重命名、删除、筛选和为 UP 主设置分组。`:app:testDebugUnitTest`、`:app:lintDebug`、`:app:assembleDebug` 已通过。剩余工作是把分组直接接入首页聚合，并补齐进程重建后的页面状态恢复。

完成标准：用户可以在一个入口中搜索或选择 UP 主、管理关注关系和首页来源；音乐库在小屏、大字号和紧凑模式下均能完整识别入口；强制停止后已选 UP、分组和页面状态按约定恢复。

### M16：UP 主系列/合集与分 P 单曲音乐库

优先级：P0。状态：进行中，合集浏览与分 P 本地歌单纵向切片已完成（2026-08-07）。

- [ ] UP 主空间增加系列、合集和歌单浏览，支持分页、整组播放和加入队列。
- [x] 建立稳定的曲目身份：单 P 使用视频本身，多 P 使用 `bvid + cid` 区分每一首歌曲。
- [x] 支持收藏单个分 P，而不必收藏整个视频；收藏信息保留分 P 标题、作者、封面和时长。
- [x] 新增本地自定义歌单，可混合保存单 P、某个分 P 和本地音乐。
- [ ] 多 P 操作补充“仅播放此 P / 从这里开始 / 加入队列 / 加入歌单”，并记住上次播放的 P 和进度。
- [ ] 本地歌单支持重命名、删除、移除曲目和基础排序；失效视频或失效分 P 显示明确状态且不阻断其余歌曲播放。

完成标准：用户可以从 UP 主系列或多 P 视频中挑选具体歌曲，保存到本地歌单并跨进程恢复；同一视频的不同 P 不会互相覆盖历史、歌词、下载或收藏状态。

当前证据：UP 主详情已接入合集/系列列表及分页详情，合集可整组播放；Room 已升级至 v8，本地歌单按 `mediaId` 保存，B 站分 P 使用 `bvid:cid`，在线临时地址不入库；多 P 列表和本机音乐均可加入歌单，歌单支持重命名、删除、移除与基础排序，并可从上次分 P 进度恢复。`:app:testDebugUnitTest`、`:app:lintDebug`、`:app:assembleDebug` 已通过。剩余工作是补齐合集“加入现有队列”和失效条目的逐项状态。

### M17：下载、离线音乐库与播放恢复

优先级：P0。状态：核心离线与错误恢复链路已完成（2026-08-07）。

- [x] 使用 `bvid + cid` 将已完成音频/视频下载与在线播放曲目关联，列表和播放器显示已下载标记。
- [x] 点击已下载内容时优先使用 MediaStore `content://` 地址；在线队列断网或地址失效时自动回退到对应本地副本。
- [x] 下载 Tab 支持直接播放、定位到对应歌曲或歌单，并明确区分音频和视频成品。
- [x] 播放异常区分地址过期、断网、超时、HTTP 错误和解码器错误，分别执行刷新地址、重试、离线回退或解码恢复。
- [x] 增加有限次数和退避策略，失败后保留当前进度、队列和播放意图，并向用户提供明确的重试入口。
- [ ] 评估播放磁盘缓存和下一首预缓存；缓存必须限制容量、支持清理，并且不与用户主动下载的永久文件混淆。
- [x] 音视频或画质切换失败时保留旧播放模式，不能因为切换失败中断原音频播放。

完成标准：已下载歌曲在无网络状态下可从原收藏、历史、歌单和队列继续播放；在线到离线切换不丢失进度；网络错误不会错误进入解码器恢复；缓存和永久下载边界清晰。

当前证据：播放请求、恢复队列和播放错误恢复均按 `bvid + cid` 查询已完成下载，优先使用可读 `content://`；下载 Tab 可直接播放音频或视频成品，本地 MP4 可在听歌/视频模式间切换；Media3 2xxx I/O 错误不再进入解码器恢复，而是依次尝试本地副本、地址刷新和最多三次短退避。播放磁盘缓存仍保留为独立评估项，未与永久下载混用。

### M18：发现、搜索、队列与连续收听

优先级：P1。状态：进行中，搜索分页与快捷队列入口纵向切片已完成（2026-08-08）。

- [ ] 首页增加“最新发布 / 未播放 / 最近播放”聚合视图，可按全部来源或 UP 主分组筛选。
- [x] 推荐页视频搜索支持服务端分页、上滑加载更多、跨页去重，并阻止旧关键词响应覆盖当前结果。
- [ ] 搜索统一支持歌曲视频、UP 主、系列/合集和本地歌单，补充搜索历史和发布时间等排序方式。
- [ ] 推荐搜索文案与实际结果类型保持一致；返回推荐后恢复原 Tab 和列表位置。
- [x] 迷你播放器增加播放队列入口和队列数量，可在当前页面直接弹出列表并完成选曲、设为下一首、移除和清空。
- [x] 队列支持任意上移/下移、保持当前播放锚点并保存为本地歌单；现有“设为下一首”继续保留。
- [ ] 队列补充拖动排序和批量移除，排序与选择操作需要兼顾 TalkBack。
- [x] 增加睡眠定时：15/30/60 分钟、当前歌曲结束和当前队列结束后停止。
- [ ] 在线历史和本地历史合并为统一历史入口，再按来源筛选；清空操作必须明确影响范围。
- [ ] 本地音乐播放前明确“仅播放此曲 / 播放当前目录”，并显示将加入队列的曲目数量。

完成标准：用户可以快速找到新歌和未听内容，搜索结果可连续加载，队列从首页直接可达且编辑后可恢复；睡眠定时在后台、锁屏和进程重建边界下行为明确。

当前证据：`/x/web-interface/wbi/search/type` 已返回带页码和 `hasMore` 的搜索页，ViewModel 保存下一页、区分首屏与续页加载，并通过请求代际校验消除快速换词和清空后的迟到响应；推荐页复用现有列表触底机制连续加载。迷你播放器新增带数量标记的播放列表入口，复用当前 MediaController 队列与 Room 持久化链路，不创建第二套播放状态。队列顺序调整同时更新 MediaController 与恢复快照，当前歌曲与进度不随索引变化；整条队列可按稳定媒体 ID 写入现有本地歌单。睡眠定时通过 DataStore 保存模式或绝对截止时间，由 `PlaybackService` 在后台和锁屏状态执行暂停，进程重建后会按剩余时间恢复，完成后保留队列和进度。

Redmi 真机证据：`wsvwypiz7xwslvl7` 定向覆盖安装后，迷你播放器显示“播放列表，共 1 首”，可直接弹出当前队列并进入“保存为本地歌单”流程；正在播放页可见 15/30/60 分钟、当前歌曲结束和当前队列结束五种睡眠选项。设置 15 分钟后顶栏显示“约 15 分钟后停止”，强制停止并冷启动后仍恢复同一截止时间；验收结束后已取消测试定时，`logcat -b crash` 为空。模拟器未参与本轮安装与操作。

### M19：歌词与播放交互打磨

优先级：P1。状态：待推进。

- [ ] 已有歌词缓存时点击歌词按钮直接展示；缓存缺失时才打开歌曲名和歌手确认弹层。
- [ ] 保留歌词手动触发原则，不在打开播放页或切歌时自动请求第三方歌词接口。
- [ ] 增加歌词时间偏移、字号调整和翻译歌词展示；偏移和字号按歌曲或全局策略持久化。
- [ ] 多 P 继续使用当前 P 名称搜索歌词，单 P 使用资源标题，不恢复 Bilibili 字幕接口。
- [ ] 多 P 弹层明确区分“全部从头播放”和“从当前 P 开始”，后台渐进解析时展示轻量队列加载状态。
- [ ] 播放、下载、离线、已加入队列和失败状态使用稳定的图标与文案表达，不能只依赖颜色。
- [ ] 复核所有播放器、弹层和紧凑列表的 `48dp` 触控区、无障碍描述、亮暗主题对比和大字号布局。

完成标准：缓存歌词一步可达；歌词调整跨重启保持且不影响播放；多 P 操作无歧义；TalkBack、大字号、亮色和暗色主题均可完成核心听歌流程。

### M20：可选增强能力

优先级：P2。状态：待评估，不阻塞前述阶段。

- [ ] 仅在视频模式评估 Android 画中画；音频后台继续使用 MediaSession，不申请悬浮窗权限。
- [ ] 评估低功耗音频频谱，并提供关闭选项；不得影响后台播放稳定性和续航。
- [ ] 评估播放器封面背景、歌词颜色和强调色定制，保持现有语义主题和亮暗对比。
- [ ] 评估自定义下载目录、存储空间统计和缓存清理。
- [ ] 有明确驾车使用需求后再评估 Android Auto 媒体浏览与语音控制。

完成标准：每项能力必须先独立证明使用价值、权限边界、功耗和稳定性，再决定是否进入正式开发阶段。

### 明确不做或不优先

- 不恢复“稍后再看”页面入口。
- 不恢复手动高、中、低音质选项；继续默认最高可用音质并自动降级。
- 不优先实现图文、转发、评论、私信等完整社区能力，也不以真实三连扣币作为听歌主链路。
- 不做设置导入/导出。
- 不恢复 Bilibili 字幕接口作为歌词来源。
- 不优先投入应用内代理、悬浮窗权限、大量圆角/字体参数或纯装饰动画。

### 推进顺序

1. M15 收敛音乐库和 UP 主来源管理，先解决入口与信息架构。
2. M16 建立分 P 单曲和本地歌单模型，形成真正的曲目级音乐库。
3. M17 打通下载与在线播放，补齐离线和错误恢复。
4. M18 优化发现、搜索、队列和连续收听效率。
5. M19 完成歌词和播放交互打磨。
6. M20 中的能力逐项评估，只有收益明确时才进入实现。

M14 正式版质量作为横向门槛贯穿 M15-M20；任何新阶段都不能降低后台播放、锁屏控制、下载恢复、Room 迁移和在线更新的现有稳定性。

## 统一阶段门槛

- 固定运行 `:app:testDebugUnitTest`、`:app:lintDebug` 和 `:app:assembleDebug`。
- 安装到指定 Android 模拟器，检查 UI、Room、系统通知、后台服务和 crash log。
- 涉及播放、下载和账号接口时必须使用真实链路验收，不能只依赖单元测试。
- 每个切片同步更新本路线图、功能迁移矩阵和差异审计。
- 只修改、提交和推送 `android-app`；`desktop-app` 始终只读。
- 每阶段使用独立中文提交，避免未完成的下一阶段功能混入发布版本。
